# OpenTrace 下一阶段优化路线图

> 版本目标：把当前“能力完备但治理分散”的 Agent 平台，升级成“统一状态、统一事件、统一预算、统一质量门禁”的认知系统。
>
> 说明：本文档是执行导向版本，重点给出**应该先做什么、为什么、做到什么程度**，方便直接进入迭代排期。

---

## 1. 现状判断

结合 `SERVICE.md` 与项目代码，OpenTrace 已经具备完整平台骨架：

- `kernel/` 已经承担认知中枢、V5 路由、V4 编排、缓存与身份保护
- `gateway/` 已经承载完整 API 网关与对外能力入口
- `agents/` 已经覆盖数据、RAG、Web、Tool、Skills、Rules
- `kernel/data_cognition/` 已经具备 Text2SQL 全链路
- `frontend/` 已经开始支持多子问题、规则页、数据页与消息卡片
- `infra/` 已有日志、指标、追踪、审计、安全、错误体系
- 测试已经是体系化存在，而不是零散补丁

但系统目前仍然有一个共同特征：

> **能力很多，协议不够统一；链路很多，闭环不够完整；有观测，但还没有形成治理。**

所以下一阶段的核心不是继续“加功能”，而是“收敛协议、强化治理、打通闭环”。

---

## 2. 目标方向

OpenTrace 的下一阶段目标建议明确成 5 个关键词：

1. **统一**：统一事件、统一状态、统一输出、统一错误、统一预算
2. **可控**：控制 token、时延、并发、重试、工具调用和风险
3. **可验**：回答、SQL、检索、工具输出、流式事件都能验证
4. **可回放**：一次回答的决策路径可以重建
5. **可进化**：失败能进入学习管线，策略可灰度可回滚

最终目标不是“更会说话的聊天框”，而是：

> **一个能调度自身能力、解释自身行为、约束自身成本、从失败中学习的认知系统。**

---

## 3. 最重要的系统短板

### 3.1 协议不统一
当前不同模块输出格式各自为政：
- Kernel 响应
- Orchestrator 响应
- Agent 结果
- 数据查询结果
- RAG 引用结果
- 流式事件

这会导致上层复用困难，也会导致后续功能越来越难整合。

### 3.2 状态不统一
任务、证据、预算、风险、策略、失败、学习信号还没有进入统一状态机。

结果是：
- 很难解释为什么走某条链路
- 很难恢复中断的任务
- 很难把失败转成策略资产

### 3.3 质量不闭环
已有 Critic、Validation、Feedback、Audit，但还没有形成完整闭环：
- 用户满意度如何映射到系统行为
- 错误如何聚类
- 策略如何灰度和回滚
- 经验如何进入记忆和路由

### 3.4 预算不明确
当前已经有分层路由，但还缺少统一预算对象来控制：
- 时间预算
- token 预算
- 工具预算
- 并发预算
- 重试预算
- 风险预算

### 3.5 前端还没完全变成“认知面板”
当前前端已开始展示多子问题和规则能力，但还没有系统性展示：
- 任务图
- 证据图
- 预算图
- 路由命中图
- 审校和重写路径

---

## 4. 总体优化原则

后续所有优化建议统一遵循以下原则：

1. **统一入口**：所有能力都通过网关和内核进入，禁止旁路
2. **统一状态**：任务、证据、策略、预算、失败统一建模
3. **统一事件**：规划、执行、检索、工具、审校、反馈都发标准事件
4. **统一输出**：所有模块都输出可消费的标准 schema
5. **统一验证**：SQL、检索、工具、回答、事件都可验证
6. **统一降级**：任何失败都有明确 fallback
7. **统一回放**：一次回答可重建关键路径
8. **统一学习**：失败和反馈进入经验体系
9. **统一治理**：策略、Prompt、路由、模型、阈值都可版本化
10. **统一灰度**：任何策略变更都支持实验与回滚

---

## 5. P0 优先级：必须先做的事情

这些工作不做，后面的优化都很难真正稳定下来。

### P0-1 统一事件协议

#### 为什么先做
系统已经有日志、trace、audit、bus，但这些还不是统一事件协议。没有统一事件，回放、审计、学习、可视化都无法真正统一。

#### 要做什么
定义统一事件类型，例如：
- `RoutingEvent`
- `PlanningEvent`
- `ExecutionEvent`
- `RetrievalEvent`
- `ToolEvent`
- `ValidationEvent`
- `CriticEvent`
- `FeedbackEvent`
- `LearningEvent`

统一事件元信息：
- `trace_id`
- `request_id`
- `session_id`
- `user_id`
- `actor`
- `stage`
- `timestamp`
- `causation_id`
- `source`

#### 完成标准
- 网关、Kernel、Orchestrator、Agent、Data Pipeline 都能发统一事件
- 流式和同步路径都可复用同一事件定义
- 前端可以基于事件流做统一展示

---

### P0-2 统一任务-证据-策略状态机

#### 为什么先做
现在很多模块都在做“局部状态管理”，但没有统一主状态。

#### 要做什么
构建三层状态：
- **任务层**：主目标、子任务、依赖、完成条件、追问条件、失败条件
- **证据层**：来源、可信度、时效性、冲突度、覆盖度、缺口
- **策略层**：路由、预算、模型、重试、降级、审校强度

#### 完成标准
- 一次回答的关键状态能够被串起来
- Fusion / Critic / Memory / Frontend 都能读取同一状态语言
- 支持中断恢复和回放

---

### P0-3 建立请求级预算系统

#### 为什么先做
没有预算，系统就容易在复杂问题上无限展开，既贵又不稳。

#### 要做什么
统一定义：
- `time_budget`
- `token_budget`
- `tool_budget`
- `agent_budget`
- `retry_budget`
- `risk_budget`
- `confidence_target`
- `latency_budget`

#### 完成标准
- 简单问题走短链路
- 中等问题可适度扩展
- 复杂问题可并行但受预算约束
- 高风险问题自动提高审校强度

---

### P0-4 统一输出 schema 与错误信封

#### 为什么先做
如果每个模块都自己定义输出格式，前端、编排器、审校器、审计系统都会被拖慢。

#### 要做什么
统一：
- `KernelResponse`
- `AgentResult`
- `Evidence`
- `ToolResult`
- `ValidationResult`
- `CriticResult`
- `StreamEvent`
- `AppException` 错误信封

#### 完成标准
- 核心模块输出字段稳定
- `metadata` 有推荐字段约定
- 同类失败的错误格式一致

---

### P0-5 Critic 结构化失败标签

#### 为什么先做
Critic 不能只是“打分”，还要成为失败学习中心。

#### 要做什么
失败标签标准化，例如：
- 事实不足
- 证据冲突
- 推理跳步
- 工具选择错误
- 任务拆分错误
- SQL 语义偏差
- 检索不足
- 风险过高
- 应该追问
- 应该拒答
- 应该降级

#### 完成标准
- Critic 输出可被机器消费
- 失败标签可进入反馈和学习管线
- 审校结果能驱动下一轮策略变化

---

### P0-6 DataAgent 输出标准化与可解释化

#### 为什么先做
Text2SQL 是高价值能力，必须可解释、可修复、可追踪。

#### 要做什么
在数据查询结果中显式记录：
- 问题识别
- 指标映射
- 过滤条件
- JOIN 路径
- 时间窗
- SQL 生成 / 修复过程
- 执行结果
- 结论可信度

#### 完成标准
- 结果可读
- 失败可定位
- SQL 过程可追踪
- 能区分“没数据”和“没查对”

---

### P0-7 全链路观测指标统一

#### 为什么先做
没有统一指标，优化很难真正闭环。

#### 要做什么
统一统计：
- 路由命中率
- 缓存命中率
- Agent 成功率
- 重试率
- 超时率
- 回答时延
- TTFT
- 追问率
- 拒答率
- 失败标签分布
- 用户反馈分布

#### 完成标准
- 指标能按 request_id / trace_id 串联
- 每个主链路都可看性能和成功率
- 每个降级路径都可统计

---

## 6. P1 优先级：尽快推进的优化

### P1-1 Memory 从存储升级为经验系统

#### 目标
把记忆从“记录对话”变成“沉淀经验”。

#### 建议方向
- 语义记忆记录高质量答案模板
- 情节记忆记录任务完成轨迹
- 程序记忆记录成功工具链和流程
- 失败记忆记录错误模式和修复策略

#### 价值
Memory 不是历史存档，而是策略训练素材。

---

### P1-2 前端升级为认知可视化界面

#### 目标
让用户看到系统怎么想、怎么做、为什么这么答。

#### 建议增加
- 任务图
- 证据图
- 路由命中情况
- 预算消耗
- 工具调用轨迹
- Critic 判定与重写
- 数据查询 SQL / RAG 引用 / Web 来源

#### 价值
用户更容易信任系统，排障也更容易。

---

### P1-3 统一 RAG / Data / Web / Tool 输出规范

#### 目标
让不同能力的结果可以进入同一融合与审校框架。

#### 建议输出统一字段
- `answer`
- `sources`
- `evidence`
- `confidence`
- `risk`
- `trace`
- `failure_reason`

#### 价值
Fusion 更稳，前端展示更统一，审校更容易。

---

### P1-4 规则、Prompt、路由版本化治理

#### 目标
让策略变更可追踪、可实验、可回滚。

#### 建议方向
- 每条规则、每个 Prompt、每个路由策略都带版本号
- 支持灰度发布
- 支持在线对比不同策略效果

#### 价值
系统从“改代码调行为”进化为“改策略调行为”。

---

### P1-5 Agent 竞争机制

#### 目标
不再只靠固定 Agent 输出，而是支持候选比较。

#### 建议方向
- 同一任务可由多个策略候选
- 通过评分、证据和风险选择最优结果
- 允许 fallback 结果进入竞争池

#### 价值
结果质量更稳定，也更适合后续自动优化。

---

## 7. P2 优先级：战略级演进

### P2-1 世界模型持续自更新

#### 目标
让系统不只是回答问题，而是形成对“业务、任务、用户、工具、失败”的稳定内部表示。

### P2-2 事件溯源与完整回放

#### 目标
任何回答都能从事件和状态中重建出来。

### P2-3 多模型协同与 bandit 路由

#### 目标
不只写死“某任务用某模型”，而是根据历史效果动态选择。

### P2-4 自动策略试验与回滚

#### 目标
让 Prompt、路由、模型、阈值都能安全试验。

### P2-5 任务域专长自动形成

#### 目标
数据、文档、规则、工具、知识等场景逐渐沉淀成专业路径，而不是每次都从零开始推理。

---

## 8. 针对核心模块的具体建议

### 8.1 `kernel/cognitive_kernel.py`

当前内核承担太多职责，建议下一步逐步拆清边界：
- 路由与缓存可以收敛成决策层
- 身份保护独立
- 编排调用独立
- 流式事件输出独立
- 统一错误和 fallback 独立

#### 目标
让内核从“大杂烩入口”变成“轻量协调器”。

---

### 8.2 `kernel/orchestrator_v4.py`

建议补强：
- 任务图显式化
- 优先级调度
- 失败隔离
- 证据权重与冲突裁定
- 多问题顺序融合稳定性

#### 目标
让编排过程可诊断、可治理、可优化。

---

### 8.3 `kernel/data_cognition/`

建议继续增强：
- 只读安全
- SQL 生成 / 验证 / 重写闭环
- 失败模式结构化
- 查询结果解释模板

#### 目标
让 Text2SQL 成为确定性分析引擎，而不是仅仅“能生成 SQL”。

---

### 8.4 `agents/`

建议统一：
- 输入 schema
- 输出 schema
- 失败原因
- 证据和置信度
- 可恢复建议

#### 目标
让 Agent 成为“可比较、可替换、可学习”的能力单元。

---

### 8.5 `model/model_gateway/`

建议补强：
- 模型命中率统计
- 失败类型画像
- 上下文截断统一策略
- 离线 fallback 标准化

#### 目标
让模型网关成为真正的调度中枢。

---

### 8.6 `frontend/`

建议增强：
- 任务图展示
- 证据链展示
- 预算与性能展示
- 路由轨迹展示
- 审校与重写展示

#### 目标
让用户从“看答案”变成“看过程”。

---

## 9. LLM 流程优化清单

### 9.1 路由层
- 路由原因可解释
- 路由结果可回放
- 路由命中率可统计
- 路由策略可灰度

### 9.2 规划层
- 明确主目标和子任务
- 明确是否需要追问
- 明确是否需要工具 / Web / 数据
- 明确预算建议和风险等级

### 9.3 生成层
- 先草案后修订
- 事实、推断、建议分层表达
- 不确定内容要显式标注
- 适度拒答优于强行编造

### 9.4 审校层
- 审校是否缺证据
- 是否答非所问
- 是否推理跳步
- 是否风险过高
- 是否需要追问或降级

### 9.5 回复质量层
建议明确五个维度：
1. 正确性
2. 完整性
3. 可解释性
4. 可信度
5. 可行动性

---

## 10. 鲁棒性与安全性优化

### 10.1 运行时鲁棒性
- 模型超时统一降级
- Agent 失败不阻断主链路
- 流式中断可优雅恢复
- 语义缓存避免污染
- 多子问题不能被单个子任务拖垮

### 10.2 数据鲁棒性
- Text2SQL 只读、防注入、LIMIT、结果兜底
- RAG 空结果、低分结果、引用不足处理
- Web API 失败、限流、401、超时处理

### 10.3 语义鲁棒性
- 统一 query 规范化
- 同义表达归一
- 多轮上下文防漂移
- 多子问题编号与来源稳定

### 10.4 安全鲁棒性
- PII、SQL 注入、有害内容检测前置
- 高风险问题更早触发审校
- 可疑请求更早进入安全路径

---

## 11. 推荐的实施节奏

### 第一阶段：底座加固
目标：先把系统变成可测、可观测、可回放。

任务：
1. 统一事件协议
2. 统一状态机
3. 统一预算对象
4. 统一输出 schema
5. 统一错误信封

### 第二阶段：核心能力增稳
目标：提升回答、数据、检索、工具链质量。

任务：
1. DataAgent 标准化
2. RAG 输出标准化
3. Critic 结构化失败标签
4. Memory 经验化
5. V5 路由继续优化

### 第三阶段：平台化升级
目标：让系统具备策略选择、在线学习、灰度更新能力。

任务：
1. Agent 竞争机制
2. 规则 / Prompt / 路由版本治理
3. 灰度与回滚
4. 前端认知面板
5. 指标驱动优化闭环

### 第四阶段：自治化演进
目标：让 OpenTrace 向认知操作系统原型演进。

任务：
1. 世界模型自更新
2. 事件溯源
3. 多模型协同
4. Bandit 路由
5. 任务域专长自动形成

---

## 12. 一句话结论

OpenTrace 已经不是一个普通的聊天应用，而是一个正在形成中的认知平台原型。

下一阶段最重要的，不是继续堆功能，而是把系统变成：

> **事件驱动、状态统一、预算可控、质量可验、可回放、可进化的认知系统。**

---

## 13. 直接可执行的下一步

1. 定义统一事件协议和事件类型
2. 给网关、Kernel、Orchestrator、Agent、Data Pipeline 统一埋点
3. 建立任务-证据-策略三层状态模型
4. 为 RAG / Data / Tool / Web 定义统一输出 schema
5. 让 Critic 输出结构化失败标签
6. 建立请求级预算对象并贯穿执行链路
7. 让 Memory 记录策略经验而不仅是对话摘要
8. 给前端增加任务图、证据图、预算图、失败图
9. 建立策略灰度、实验和回滚机制
10. 形成可回放、可审计、可恢复的闭环

---

> 文档版本：`next_work.md`
>
> 建议维护方式：当事件协议、状态机、预算模型、质量门禁、路由策略或回复质量标准发生变化时，优先更新本文档。
